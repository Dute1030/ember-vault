import {test} from 'node:test';
import assert from 'node:assert/strict';
import {newGame,act,type Action} from '../lib/game.ts';
import {choiceConfirmation} from '../lib/choice-confirmation.ts';
test('all noncombat choices preview without consuming any run state',()=>{const s=newGame('warden',false,12);s.offers=['bolt'];s.shop=['venom'];s.event=0;s.hp=8;const original=structuredClone(s);const actions:Action[]=[{type:'node',lane:1},{type:'reward',index:0},{type:'reward',index:-1},{type:'event',index:0},{type:'event',index:1},{type:'rest'},{type:'buy',index:0},{type:'buy',index:3},{type:'remove',index:0},{type:'leave'}];for(const a of actions){const preview=choiceConfirmation(s,a);assert(preview?.title);assert(preview?.detail);assert.deepEqual(s,original)}});
test('lethal event and payment previews state exact consequences',()=>{const s=newGame('warden');s.hp=9;s.event=0;s.shop=['venom'];assert.match(choiceConfirmation(s,{type:'event',index:0})!.detail,/死亡/);assert.match(choiceConfirmation(s,{type:'buy',index:0})!.detail,/65 金币/);assert.match(choiceConfirmation(s,{type:'remove',index:0})!.detail,/55 金币/);assert.match(choiceConfirmation(s,{type:'buy',index:3})!.detail,/90 金币/)});
test('reselecting a reward adds only the explicitly confirmed card',()=>{const s=newGame('warden');s.phase='reward';s.floor=0;s.offers=['guard','strike'];choiceConfirmation(s,{type:'reward',index:0});choiceConfirmation(s,{type:'reward',index:1});assert.equal(s.deck.length,10);const n=act(s,{type:'reward',index:1});assert.equal(n.deck.length,11);assert.equal(n.deck.at(-1),'strike');assert.equal(n.phase,'map')});
test('combat actions do not require an additional confirmation',()=>{const s=newGame('warden');assert.equal(choiceConfirmation(s,{type:'play',index:0}),null);assert.equal(choiceConfirmation(s,{type:'end'}),null)});
